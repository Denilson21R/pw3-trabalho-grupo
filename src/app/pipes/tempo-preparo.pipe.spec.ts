import { TempoPreparoPipe } from './tempo-preparo.pipe';

describe('TempoPreparoPipe', () => {
  it('create an instance', () => {
    const pipe = new TempoPreparoPipe();
    expect(pipe).toBeTruthy();
  });
});
